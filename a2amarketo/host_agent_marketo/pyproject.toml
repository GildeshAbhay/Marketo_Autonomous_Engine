[project]
name = "host-agent-marketo"
version = "0.1.0"
description = "Host agent for orchestrating Marketo operations."
requires-python = ">=3.10"
dependencies = [
    "google-adk>=1.2.1",
    "a2a-sdk>=0.2.5",
    "nest-asyncio>=1.6.0",
    "python-dotenv",
    "click",
    "uvicorn",
    "google-generativeai",
    "httpx",
    "requests",
]